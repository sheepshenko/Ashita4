require('common');

local settings = T{ };
settings["decline"] = T{ };
settings["join"] = T{ };
settings["ignore"] = T{ };
settings["mode"] = 1;
settings["join"][1] = "Tamaaki";
settings["join"][2] = "Nyahnna";
settings["join"][3] = "Yohf";

return settings;
